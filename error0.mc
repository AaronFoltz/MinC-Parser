int add (int x, int y, int x) {
  if (y == 0) return (x);
  else   return (incr(add(x,y-1)));
}

int incr (int x) 
{
  { int x;  int y;
    y = x + 1;
  } 
  return (x + y);
}

int add(int x) {
  return (y);
}

int main() {
  add (x, 2); 
}

